bazel_dep(name = "probe_rs_tools", version = "0.0.0", dev_dependency = True)
bazel_dep(name = "bazel_skylib", version = "1.7.1", dev_dependency = True)

local_path_override(
    module_name = "probe_rs_tools",
    path = "../..",
)
